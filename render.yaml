services:
  - type: web
    name: predictive-maintenance-api
    env: docker
    dockerfilePath: Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: 8000
      - key: APP_ENV
        value: production
